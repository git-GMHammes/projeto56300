/**
 * row-factory — fábrica de linhas Bootstrap para Vue 3
 *
 * Importação nomeada:
 *   import { RowLayout } from '@/components/row-factory'
 *
 * Importação direta (tree-shaking melhor):
 *   import RowLayout from '@/components/row-factory/RowLayout.vue'
 */

export { default as RowLayout } from './RowLayout.vue'
